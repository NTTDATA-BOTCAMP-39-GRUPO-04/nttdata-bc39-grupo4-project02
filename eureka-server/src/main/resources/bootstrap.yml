spring:
  application:
    name: eureka-server
  cloud:
    config:
      enabled: true
      label: master
      uri: http://localhost:9097

---
spring.config.activate.on-profile: docker
spring:
  cloud:
    config:
      enabled: true
      label: master
      uri: http://config-server