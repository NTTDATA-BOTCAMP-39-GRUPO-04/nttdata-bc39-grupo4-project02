spring:
  application:
    name: composite-service
  cloud:
    config:
      enabled: true
      label: master
      uri: http://localhost:9097

app:
  account-service:
    hostname: localhost
    port: 9092

  movements-service:
    hostname: localhost
    port: 9095